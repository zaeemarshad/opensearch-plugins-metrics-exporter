// Package collector provides Prometheus collector implementations for OpenSearch plugins.
// Each plugin has its own sub-package (e.g., knn, neural) that implements the
// prometheus.Collector interface to expose plugin-specific metrics.
package collector
